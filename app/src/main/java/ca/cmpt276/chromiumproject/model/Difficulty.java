package ca.cmpt276.chromiumproject.model;

/**
 * Difficulty is a public enum that defines the Difficulty levels of a GameRecord.
 * Used to calculate Achievement levels.
 *
 * Has built-in name() function, which returns the enum's name as a string!
 */

public enum Difficulty {
    NORMAL,
    EASY,
    HARD
}